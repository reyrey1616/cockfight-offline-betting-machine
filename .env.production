# Copy to `.env.production` before building the Electron kiosk installer.
# Multiple computers: one SERVER PC runs the API; every TELLER kiosk PC uses this same LAN IP.
# See ../MULTI-COMPUTER.md

VITE_API_BASE_URL=http://192.168.1.6:8000
